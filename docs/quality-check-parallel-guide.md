# 品質チェックの進め方（フロント・バックエンド・DB 3セクション並行実施）

## 全体の流れ

1. ユーザーが新規チャットを3つ開く（フロントエンド用・バックエンド用・DB用）
2. それぞれのチャットで `/quality-check` を実行し、担当領域の品質チェックを行う（**チェックのみ実行し、削除等の後片付けは行わない**。後片付けは別セッションで行う）
3. 各チャットは、チェック結果を`docs/`配下に以下のファイル名でドキュメント化する（ローカル保存のみ、コミット・プッシュはしない）
   - `docs/quality-frontend.md`
   - `docs/quality-backend.md`
   - `docs/quality-db.md`
4. マネージャー役のこのセッションで、後ほど上記3ファイルをローカルで確認する
5. 3つの内容をこのファイルに統合し、指摘事項の優先度確認・Issue化・修正を進めた上で、個別ファイルは削除しこのファイルのみリモート（GitHub）にプッシュする

## なぜ新規チャットで実行するのか

実装済みのセッションでそのまま品質チェックをすると、セルフレビューバイアスがかかりやすい。
- 実装した本人（同じセッション）は無意識に自分のコードに甘くなる
- 「知っているから大丈夫」で細部の確認を省略しがちになる

第三者によるコードレビューが重視されるのと同じ理由で、品質チェックは実装内容を知らない新規セッションで行う。

## 3セクション並行作業時の注意点

3つとも「読み取り・診断のみ」（lint・ビルド確認・コードを読んでの確認・DBコンテナの状態確認）でファイルを書き換えないため、git上の競合は基本的に発生しない。ただし以下3点に注意する。

1. **足りないツールを勝手にインストールしない**
   - フロントエンドに`eslint`、バックエンドに`ruff`/`flake8`/`mypy`が未導入
   - 3セッションが同時に`npm install`や`pip install`を実行すると、共有の`node_modules`や仮想環境への同時書き込みで競合する可能性がある
   - 「ツールが無い」ことは指摘事項として報告してもらい、インストールの判断はマネージャー役のセッションでまとめて行う
2. **Dockerコンテナを止める操作はしない**
   - 状態確認（`docker compose ps`、ログ確認、コンテナ内での`alembic current`実行など）はOK
   - `docker compose restart` / `down` のような操作は他セッションの動作確認にも影響するため避ける
3. **`npm run build` はやや重い処理**
   - 3セッション同時実行でPCがもたつく可能性はあるが、致命的な問題にはならない想定

## 参照

- 品質チェックコマンド本体: `~/.claude/commands/quality-check.md`（バックアップ: `https://github.com/kmrtk/claude-config` の `commands/quality-check.md`、ローカルクローン: `C:/Users/kimur/Takacursor/claude-config`）

---

# 品質チェック結果（統合）2026-07-21

3セッション（フロントエンド・バックエンド・DB）の実施結果を統合したもの。個別ファイル（`docs/quality-frontend.md`等）はローカル作業用の一時ファイルだったため、統合後に削除済み。

## 総評

3領域とも重大（Critical/High）な問題は検出されなかった。フロントエンド・バックエンドでMedium優先度の指摘が1件ずつ、残りはLowまたは対応不要（設計として正しい／ライブラリ側の警告）。

## チェック結果サマリー

| 領域 | Lint | 型チェック | ビルド/テスト | コードパターン | 共通（シークレット・デバッグコード等） |
|---|---|---|---|---|---|
| フロントエンド | ⚠️ ESLint未導入 | - (TS未使用) | ✅ build成功 / ✅ vitest 8件パス | ⚠️ 下記参照 | ✅ 問題なし |
| バックエンド | ⚠️ ruff/flake8/mypy未導入 | ⚠️ 同上 | ✅ pytest 11件パス | ⚠️ 下記参照 | ✅ 問題なし |
| DB/インフラ | - | - | ✅ マイグレーション整合性OK | ✅ 問題なし | ✅ 問題なし |

## 問題点一覧（優先度順・統合）

### [Medium]（すべて対応済み）

1. ✅ **（フロント）初期データ取得のエラーハンドリングがない** — `CalendarView.vue`の`fetchMonthData`、`ExpensesView.vue`の`fetchExpenses`、`ScheduleListView.vue`の`fetchSchedules`、`YearlyBudgetView.vue`の`fetchYearData`/`fetchAllTimeSummary`にtry/catchがなく、API通信失敗時にUnhandled Promise Rejectionとなり、ユーザーに何もエラー表示されない → Issue #64 / PR #65で対応、マージ済み
2. ✅ **（バックエンド）会員登録パスワードに長さ・強度の制約がない** — `backend/app/schemas/auth.py`の`RegisterRequest.password`が単なる`str`で、空文字列や1文字でも登録できてしまう → Issue #66 / PR #67で対応（英数字8文字以上のバリデーションを追加）、マージ済み
3. ✅ **（フロント）ESLint未導入** — 静的解析の仕組みがなく、レビュー頼みになっている → Issue #68 / PR #69で対応（`eslint`+`eslint-plugin-vue`の`flat/essential`ルールセットを導入）、マージ済み

### [Low]

4. （フロント）401エラー時に自動でログイン画面へ遷移しない
5. （フロント）アイコンのみのボタンに`aria-label`がない（電卓ボタン以外）
6. （バックエンド）`mypy`/`ruff`/`flake8`未導入
7. （バックエンド）`@app.on_event("startup")`が非推奨API（`lifespan`への移行が将来的に必要）
8. （バックエンド）CORSオリジンがハードコード（本番デプロイ時に環境変数化を検討）
9. （DB）`budgets.py`の年間・累計集計が全件取得＋Python側集計（SQLの`SUM`/`GROUP BY`未使用）。個人利用規模では実害なし
10. （DB）`Schedule`モデルに`expense`への逆参照`relationship`がない。現状未使用のため実害なし

### 対応不要（参考として記載）

- （フロント）Piniaストアのstate直接代入 — Options Store actionsの正しい書き方
- （バックエンド）`datetime.utcnow()`非推奨警告 — 依存ライブラリ（python-jose）内部の警告で自プロジェクトのコード対象外

## 次のアクション（要判断）

Medium 3件は対応済み（上記参照）。Low 7件・lintツール導入系（mypy/ruff/flake8）は着手するかどうか未判断。着手する場合はマネージャー役セッションでユーザーと相談の上、Issue化する。
